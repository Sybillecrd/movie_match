<% if @next_trailer %>
  // insérer dans carousel-inner le partial du next trailer
  $('.carousel-inner').append('<%= j render "trailers/trailer", trailer: @next_trailer, active: false %>');

  // slide carousel
  $('.carousel').carousel("next");

  // delete previous slide
  setTimeout(function(){
    $('.carousel .item').first().remove();
  }, 1000);
<% else %>
  alert('no more movies');
<% end %>
